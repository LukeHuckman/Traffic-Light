	ORG	00H
	AJMP	MAIN

MAIN:	MOV	A,#00H
	MOV	P0,A

START:	MOV	A,#01H
	MOV	P0,A
	ACALL	DELAY
	MOV	A,#02H
	MOV	P0,A
	ACALL	DELAY
	MOV	A,#04H
	MOV	P0,A
	ACALL	DELAY
	AJMP	START

DELAY:	MOV	R1,#0FFH
DELAY1:	MOV	R2,#0F0H
DELAY2:	DJNZ	R2,DELAY2
	DJNZ	R1,DELAY1
	RET

	END